<Window x:Class="CRMApp.ClientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление / Редактирование клиента" 
        Height="412" 
        Width="475"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок формы -->
        <TextBlock Grid.Row="0" 
                   Text="📋 Данные клиента" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- ФИО -->
        <TextBlock Grid.Row="1" 
                   Text="ФИО:" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>
        <TextBox x:Name="txtFullName" 
                 Grid.Row="2" 
                 Margin="0,0,0,15"
                 Height="35"
                 FontSize="14"/>

        <!-- Телефон -->
        <TextBlock Grid.Row="3" 
                   Text="Телефон:" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>
        <TextBox x:Name="txtPhone" 
                 Grid.Row="4" 
                 Margin="0,0,0,15"
                 Height="35"
                 FontSize="14"/>

        <!-- Email -->
        <TextBlock Grid.Row="5" 
                   Text="Email:" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>
        <TextBox x:Name="txtEmail" 
                 Grid.Row="6" 
                 Margin="0,0,0,25"
                 Height="35"
                 FontSize="14"/>

        <!-- Кнопки (здесь они ЕСТЬ!) -->
        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button x:Name="btnOk"
                    Content="✅ Добавить / Сохранить" 
                    Width="150" 
                    Height="40" 
                    Margin="5" 
                    Background="LightGreen"
                    FontWeight="Bold"
                    Click="BtnOk_Click"/>
            <Button x:Name="btnCancel"
                    Content="❌ Отмена" 
                    Width="100" 
                    Height="40" 
                    Margin="5" 
                    Background="LightCoral"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>